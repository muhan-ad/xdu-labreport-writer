# 主应用修复与评估

日期：2026-09-14  
项目：`E:\agent-project\labreport-writer`  
范围：本轮复查提出的六项问题，以及相关回归测试、使用说明文字同步。

## 修改前备份

- 目录：`D:\back\labreport-writer-20260914-194421`
- 包含项目源码、Git 历史、本地依赖、Python 运行时及原有构建产物。
- 共 30,604 个文件，约 1.68 GB；复制失败数为 0。
- 修改前已执行全目录比对，未发现遗漏或差异；六个关键文件的 SHA256 与备份一致。
- 备份是修改前状态，不包含本次新增文件。如需恢复，先保留修改后的目录，再从备份复制，避免直接覆盖后无法找回新内容。

## 完成的修复

| 项目 | 最终行为 |
| --- | --- |
| 实验资源热更新失败 | 先构建完整候选目录；合并失败不改变正在使用的数据。切换目录失败时恢复旧目录，不再把失败标为更新成功。数据版本信息随资源目录一起提交。 |
| 重复点击当前实验 | 直接保留当前表单；保存及实验加载期间锁定切换，避免重新加载覆盖尚未保存的输入。 |
| AI 多章节原文混用 | 开始任务时固定实验路径与全部章节原文；后续切换实验不改变本次请求输入，结果仍绑定来源实验。禁止重入同一润色流程。 |
| 重复启动报告生成 | 在自动保存前锁定生成按钮和状态；主进程再次检查互斥，阻止并发生成及生成期间的数据更新。保存失败会恢复界面状态。 |
| Word 进程归属 | 从本次文档窗口取得 HWND 与 PID；正常结束使用持有的进程句柄，取消时再次核对窗口与进程。移除全局 PID 差集清理逻辑。无法确认身份时不强行清理其他进程。 |
| 安装升级后的资源同步 | 按内置资源内容指纹更新用户目录中已有实验的程序资源；保留测量数据、报告及用户修改的变体。同一安装资源指纹下不反复覆盖已应用的热更新。 |

同时调整：

- 成功生成报告后才更新对应章节缓存，失败或取消不覆盖旧报告的原文缓存。
- Python 标准输出使用流式 UTF-8 解码，减少跨数据块中文被截断的情况。
- 使用说明删除“部分实验暂无变体”，补充生成前自动保存及 Word 清理的实际行为。
- 新增 `npm test`，便于后续重复执行本次回归检查。

## 评估结果

**结论：本轮六项问题已完成修复，并通过下列验证；尚不能等同于完整安装包发布验收。**

### 自动回归：12 / 12 通过

执行：`npm test`

1. 资源与版本信息共同提交，保留上一份恢复副本。
2. 候选目录构建失败时旧资源、旧版本保持不变。
3. 目录重命名失败时恢复旧目录和旧版本。
4. 安装升级更新脚本及公共库，同时保留测量数据、报告和自建变体；后续同指纹同步不覆盖热更新。
5. 点击当前实验不重载未保存数据。
6. 自动保存期间重复点击生成只启动一次。
7. 自动保存异常不启动 Word，并解锁界面。
8. 多章节润色期间切换实验，后续章节仍使用来源实验的快照。
9. 主进程拒绝并发生成；取消只提交本任务记录的 Word 身份。
10. 生成期间拒绝数据更新；已有旧 DOCX 不被判为本次生成成功。
11. 模拟解压完成后，真实文件合并和版本提交成功。
12. 模拟更新复制失败后，完整旧目录和版本仍保留。

测试文件：`tests/regression.test.js`。文件操作发生在独立临时目录；AI 网络调用、Electron 窗口和 Python 子进程在这些自动回归场景中使用替代实现。

### 实际 Microsoft Word 验证：通过

执行：

```powershell
node tests/word_integration.js "D:\back\labreport-writer-evaluation-20260914-194421"
```

- 使用项目内置 Python 运行时与本机 Microsoft Word。
- 成功生成包含中文正文和 Word 原生公式的 DOCX，文件内部结构检查通过。
- 生成开始后另建一个独立 Word 实例，原报告结束后，该实例中的文本仍可正常访问。
- 使用主应用实际采用的取消清理程序，错误的 PID / HWND 配对被忽略；正确配对只结束指定实例，独立 Word 实例保持正常。
- 测试中创建的独立 Word 实例已关闭；未连接或修改用户原有文档。
- 测试报告：`D:\back\labreport-writer-evaluation-20260914-194421\Word回归测试.docx`。

实测期间发现本机 Word 不支持 `Word.Application.Hwnd`，已改为从 `ActiveWindow.Hwnd` 取得身份，修正后复测通过。

### 静态检查：通过

- 26 个实验通过原有 `smoke_test.py` 检查，异常数为 0。
- 主进程、界面脚本及两个新增模块通过 JavaScript 语法检查。
- `git diff --check` 未发现空白格式错误。

## 主要修改文件

- `main.js`：任务互斥、更新事务、安装资源同步、Word 身份接收及取消处理。
- `src/main/resource-store.js`：候选资源目录、回滚与资源指纹迁移。
- `src/main/word-process.js`：Word 身份核对和清理程序。
- `src/renderer.js`：表单与任务锁、润色原文快照。
- `物理实验/实验脚本/common/docx_report.py`：持有本实例的 Word 进程句柄。
- `src/index.html`：设置中使用说明的文字更新。
- `package.json` 与 `tests/`：回归测试入口和 Word 集成验证。

## 验证边界与后续使用

- 未连接真实 AI 服务，也未从线上下载更新包；自动回归在这些外部边界使用模拟。
- 已实际验证 Word 公共生成器与清理程序，但未逐一生成全部 26 个实验报告，也未完整操作桌面界面的所有入口。
- 未重新构建或分发安装包。源码运行可通过 `npm start` 查看；安装版需另行打包安装才能使用本轮修改。
- 资源迁移会保留旧目录副本，位于用户数据目录中以 `.resource-` 开头的目录下的 `previous` 子目录。它们会占用额外空间，确认稳定后可自行归档；本轮没有自动删除这些恢复副本。
- 首次迁移缺少历史基准的变体文件会保守保留，避免把用户改写误认作旧模板覆盖。
- 突然断电、强制结束应用、磁盘损坏等中断情形未进行故障注入验证；这类情况可使用修改前完整备份或保留的资源副本恢复。

## 使用说明编辑位置

打开 `src/index.html`，搜索 `id="paneHelp"`。其中 `<h4>` 是小标题，`<li>` 是说明条目。样式位于 `src/style.css` 的“设置 · 使用说明页”区块。
