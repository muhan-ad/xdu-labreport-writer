# 1.7.6 打包记录

日期：2026-09-15。

## 安装包

- Windows x64 NSIS 安装器：`dist/实验搭子 Setup 1.7.6.exe`
- 大小：172550784 字节，约 165 MiB。
- SHA-256：`A812F32F43F28F489B55C8159C6FEE4B03CFCEEA5C1A09FD6863C4D328479002`
- 按维护者要求不使用 Windows 程序签名，实测状态为 `NotSigned`。
- 使用 `electron-builder --win nsis --x64 --publish never` 构建成功。
- 包内版本为 1.7.6，反馈使用新的 `files` 协议；预览组件、安全模块和更新公钥齐全，未包含发布私钥。
- 安装包保留本地供网盘分发，不作为 Git 源码文件提交，也未自动发布 GitHub Release。

## 清理

旧 1.7.5 安装包、旧 `dist/win-unpacked`、旧安装版本清单及临时调试文件已移出常用构建目录，归档至仓库根目录 `backup-old-builds-20260915/`。永久删除命令被自动审批阻止，因此采用可恢复归档，未释放其磁盘空间。归档被 Git 忽略。

`dist/win-unpacked` 已重新生成，为 1.7.6。正在使用的 `dist/verification-1.7.6` 验证程序以及原实验数据 ZIP/清单保留；原实验清单为历史产物，不能作为新签名更新清单直接发布。用户数据未清理。

## 验证及范围

- `npm run verify` 通过：27 项 Node 测试、26 项实验静态检查、26 项 schema 校验、4 项计算回归。
- 生产依赖审计：0 个已知漏洞。
- 提交文件常见密钥模式检查：无发现。
- 最终安装器尚未执行另一台电脑上的安装验收。
- 用户已配置云函数；线上空列表 POST 探测到文件列表校验响应，但真实贡献/反馈上传验收尚未确认。
- 管理端签名发布适配仍属于待完成事项，详见 `RELEASE.md`。

本记录补充此前审查、修复报告中的构建及部署状态；GitHub 推送以仓库提交记录为准。
